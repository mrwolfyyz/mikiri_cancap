# Requires Python 3.10+ (codebase uses modern type syntax like float | None).
# CI runs on 3.12; locally use: python3.13 -m pytest
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = -v --tb=short --cov=gcp --cov-report=term-missing
